#include<stdio.h>
int main()
{
	long n1, n2,i,sum=0;
	double avg, s_d;
	scanf("%ld",&n1);
	scanf("%ld", &n2);
	if (n1 < n2)
	{
		for (i = n1; i <= n2; i++)
		{
			printf("%ld ", i);
			sum+=
		}

	}

}